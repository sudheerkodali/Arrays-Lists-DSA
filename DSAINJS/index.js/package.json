
const ResizableArray = require('./arrays/ResizableArray');

arr = new ResizableArray();

arr.push(10);
arr.push(20);
arr.push(30);
arr.push(1);
arr.push(40);

for (let i = o; i < arr.size(); i++) {
    console.log(arr.get(1));
}
